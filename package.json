{
  "name": "info",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "overrides": {
      "follow-redirects": ">=1.16.0"
    }
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "4"
  }
}
